#include <stdio.h>
#include <stdlib.h>

int main() {
	// %s instructs printf to read a second argument as a string
	// \n adds a new line after the %s
	printf("%s\n", "Hello, world!");
	return EXIT_SUCCESS;
}
